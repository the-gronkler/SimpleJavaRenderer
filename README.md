Simple Rendering program written in Java:

Features - full rendering capabilities(translation, rotation, scaling, model clipping), dynamic shading, procedural sphere generation. 
The rendering is implemented based on a simple orthogonal projection, so depth perception is absent
